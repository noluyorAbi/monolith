node_modules
.next
# Any .env, because cp .env.example .env is the natural setup step and the
# first line of that template is a GitHub token.
.env*
!.env.example
.data
*.stl
.DS_Store
tsconfig.tsbuildinfo
.remotion
.vercel
